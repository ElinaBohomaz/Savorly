<Page x:Class="Savorly.Views.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Savorly - Пошук" 
      Background="White">

    <Page.Resources>
        <Style x:Key="FavoriteHeartStyle" TargetType="Border">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="15,15,15,0"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                    <Setter Property="Background" Value="#FF6B00"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FavoriteIconStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Text" Value="🤍"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                    <Setter Property="Text" Value="❤️"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="White" Padding="30,20">
            <StackPanel>
                <TextBlock Text="Пошук рецептів" FontSize="28" FontWeight="Bold" Foreground="#333" Width="229"/>
                <TextBlock Text="Знайдіть ідеальний рецепт" FontSize="16" FontWeight="Bold" Foreground="#666" Margin="0,8,0,0" Width="217"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Background="White" Padding="30,0,30,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#F8F9FA" CornerRadius="12" BorderBrush="#E8E8E8" BorderThickness="1">
                    <TextBox x:Name="SearchTextBox" 
                             Height="50" 
                             FontSize="16" 
                             Padding="20,0"
                             VerticalContentAlignment="Center"
                             BorderThickness="0"
                             Background="Transparent"
                             TextChanged="SearchTextBox_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="#333"/>
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="Background" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Border>
                <Button Grid.Column="1" 
                        Content="🔍" 
                        FontSize="18"
                        Background="#FF6B00" 
                        Foreground="White"
                        BorderThickness="0" 
                        Margin="15,0,0,0"
                        Width="50" 
                        Height="50"
                        Click="SearchButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
        <Border x:Name="FiltersBorder" Grid.Row="2" Background="White" Padding="30,0,30,20">
            <StackPanel>
                <TextBlock Text="Фільтри:" FontSize="18" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,12"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" 
                            Content="‹" 
                            FontSize="20" 
                            FontWeight="Bold"
                            Background="White"
                            Foreground="#FF6B00"
                            BorderThickness="1"
                            BorderBrush="#E8E8E8"
                            Width="40"
                            Height="40"
                            VerticalAlignment="Center"
                            Click="ScrollTagsLeft_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <ScrollViewer Grid.Column="1" 
                                  HorizontalScrollBarVisibility="Hidden" 
                                  VerticalScrollBarVisibility="Disabled"
                                  x:Name="TagsScrollViewer"
                                  Margin="15,0">
                        <StackPanel x:Name="TagsPanel" Orientation="Horizontal">
                            <Border Background="#FF6B00" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#усі" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#сніданок" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#обід" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#вечеря" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#десерт" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#коктейлі" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#смузі" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#швидко" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#здорове" FontSize="14" Foreground="#333"/>
                            </Border>
                            <Border Background="#F0F0F0" Padding="20,10" Margin="8,0" CornerRadius="20"
                                    MouseLeftButtonDown="FilterTag_Click">
                                <TextBlock Text="#вегетаріанське" FontSize="14" Foreground="#333"/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                    <Button Grid.Column="2" 
                            Content="›" 
                            FontSize="20" 
                            FontWeight="Bold"
                            Background="White"
                            Foreground="#FF6B00"
                            BorderThickness="1"
                            BorderBrush="#E8E8E8"
                            Width="40"
                            Height="40"
                            VerticalAlignment="Center"
                            Click="ScrollTagsRight_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <RadioButton x:Name="AllTypesRadio" Content="Усі" GroupName="TypeFilter" 
                               IsChecked="True" Checked="TypeFilter_Checked" 
                               FontSize="14" Margin="0,0,20,0"/>
                    <RadioButton x:Name="FoodTypeRadio" Content="🍳 Страви" GroupName="TypeFilter" 
                               Checked="TypeFilter_Checked" FontSize="14" Margin="0,0,20,0"/>
                    <RadioButton x:Name="DrinkTypeRadio" Content="🥤 Напої" GroupName="TypeFilter" 
                               Checked="TypeFilter_Checked" FontSize="14"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="ResultsTitle" Text="Всі рецепти" 
                          FontSize="18" FontWeight="SemiBold" Foreground="#333"
                          Margin="30,10,30,20" Visibility="Visible"/>

                <ItemsControl x:Name="SearchResultsItemsControl" Margin="30,0,30,30">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="320" Height="380" Background="White" Margin="15,15" 
                                    BorderBrush="#E8E8E8" BorderThickness="1" CornerRadius="12"
                                    MouseLeftButtonDown="RecipeCard_Click">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="3" Opacity="0.1" BlurRadius="12"/>
                                </Border.Effect>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="160"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="#FFF9F5" CornerRadius="12,12,0,0">
                                        <Grid>
                                            <Image Source="{Binding ImagePath}" 
                                                   Stretch="UniformToFill" 
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"
                                                   Height="140">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                            <TextBlock FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="Food">
                                                                <Setter Property="Text" Value="🍳"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Type}" Value="Drink">
                                                                <Setter Property="Text" Value="🥤"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <Border Style="{StaticResource FavoriteHeartStyle}"
                                                    MouseLeftButtonDown="FavoriteButton_Click">
                                                <TextBlock Style="{StaticResource FavoriteIconStyle}"/>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <StackPanel Grid.Row="1" Margin="20,15">
                                        <TextBlock Text="{Binding Title}" 
                                                   FontWeight="SemiBold" 
                                                   FontSize="16" 
                                                   Foreground="#333" 
                                                   TextWrapping="Wrap" 
                                                   MaxHeight="45"
                                                   LineHeight="20"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🕒" FontSize="12" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding PreparationTime}" FontSize="12" 
                                                           Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                                                <TextBlock Text="хв" FontSize="11" Margin="2,0,15,0" 
                                                           VerticalAlignment="Center" Foreground="#666"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👥" FontSize="12" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Servings}" FontSize="12" 
                                                           Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="Medium"/>
                                                <TextBlock Text="порц" FontSize="11" Margin="2,0,0,0" 
                                                           VerticalAlignment="Center" Foreground="#666"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <TextBlock Text="{Binding Tags}" 
                                                   FontSize="12" 
                                                   Foreground="#FF6B00" 
                                                   Margin="0,8,0,0"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <Border Grid.Row="2" Background="#FF6B00" 
                                            CornerRadius="0,0,12,12" 
                                            Height="45">
                                        <TextBlock Text="Детальніше" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="White"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border x:Name="NoResultsMessage" Visibility="Collapsed" 
                        Background="White" Margin="30" Padding="40">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="🔍" FontSize="64" HorizontalAlignment="Center" Foreground="#CCC"/>
                        <TextBlock Text="Рецептів не знайдено" FontSize="20" FontWeight="SemiBold" 
                                  Foreground="#666" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                        <TextBlock Text="Спробуйте змінити запит або фільтри" FontSize="14" 
                                  Foreground="#999" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>